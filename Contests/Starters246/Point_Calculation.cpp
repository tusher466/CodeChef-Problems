#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    
    int total_points = (a * 3) + (b * 1) + (c * 0);
    
    cout << total_points << endl;

    return 0;
}
