#include <bits/stdc++.h>
using namespace std;

int main() {
    
    int x, y;
    cin >> x >> y;

    if (y <= x) {
        cout << 0 << endl;
    } else {
        cout << 100 << endl;
    }

    return 0;
}
